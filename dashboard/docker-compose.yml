version: '3.8'

services:
    admin-dashboard:
        build:
            context: ./
            dockerfile: Dockerfile
        ports:
            - '3000:80'

networks:
    network:
        driver: bridge
